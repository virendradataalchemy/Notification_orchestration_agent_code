# Tests

This folder is for automated test coverage that can be run with `pytest`.

Current automated suites:

- `test_sdk_notifications.py`
- `test_sdk_inbound.py`

Manual verification and operational helper scripts live under `scripts/manual/`.
