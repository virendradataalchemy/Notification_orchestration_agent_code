# Minimal requirements for Docker deployment

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
# Optional database tooling kept out of Docker runtime for faster builds.
# Uncomment only when you explicitly run migrations or sync DB scripts inside containers.
# alembic==1.13.3
# psycopg2-binary==2.9.9

# Cache & Queue
redis==5.0.8
hiredis==2.3.2
celery[redis]==5.4.0

# AWS Services (minimal)
boto3==1.35.0

# External Providers (core only)
twilio==9.3.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.0

# Template Engine
jinja2==3.1.4

# Utilities
python-dotenv==1.0.1
email-validator
phonenumbers
colorama>=0.4.6

# Optional provider SDKs not required for the current Docker runtime.
# Uncomment when those channels/features are actively used in containers.
# slack-sdk
# firebase-admin

# Optional AI / agentic routing layer (disabled for current internal-module deployment)
# Uncomment only when re-enabling self-learning routing / ChromaDB memory.
# chromadb>=0.5.23

# HTTP Client
aiohttp==3.10.0

# Optional runtime helpers currently unused in the active Docker path.
# httpx==0.27.0
# python-dateutil==2.9.0
# pytz==2024.1

# Logging
python-json-logger==2.0.7
